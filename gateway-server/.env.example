# Gateway Server Configuration

# Port to run the HTTP server on
PORT=6767

# SSH username for connecting to host machine
# This should be your macOS username
SSH_USER=your_username_here

# Maximum number of active sessions allowed per creator (default: 10)
MAX_SESSIONS_PER_CREATOR=10

# Database path (inside container)
# External mount point should be configured in docker run command
# DB_PATH=/data/sessions.db
